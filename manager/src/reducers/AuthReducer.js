//  Set initial state
const INITIAL_STATE = { email: '' };

//  Create and export reducer
export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    case 
    default:
      return state;
  }
}
